# CLAUDE.md

## Project Overview

Project codename: `tracepoint-m`

A modern artifact repository and software supply-chain platform inspired by Sonatype Nexus, written in Go.

Primary goals:
- Scalability is the #1 priority
- High performance
- Operational simplicity
- Strong API-first design
- Modern RBAC/authentication
- Native OCI/container ecosystem support
- Extensible plugin system
- Strong metadata/indexing/search capabilities

This project is NOT intended to be a Nexus clone. It should modernize the architecture and UX while supporting common enterprise artifact workflows.

---

# Core Design Principles

## 1. API-first
All functionality must be accessible via:
- REST API
- CLI compatibility layer
- Web UI consuming public APIs only

The UI must never rely on hidden/private backend endpoints. ??

---

## 2. Stateless application layer
Application nodes should remain as stateless as possible.

Persistent state belongs in:
- PostgreSQL
- Object storage
- Redis (ephemeral/cache/queue) ??? (Has some operational problems, TBD on this direction) -> (Actually makes sense to cache metadata in redis, TBD)
- Search/index backend

This allows:
- Horizontal scaling
- HA deployments
- Easier upgrades

---

## 3. Flexible storage backend (2 options to begin with for TTM)
Artifacts are immutable blobs.

Support:
- S3
- Filesystem backend


---

## 4. Immutable-by-default artifacts
Published artifacts should generally be immutable.

Allow configurable policies:
- immutable
- overwrite
- append-only
- retention-based

Configurable Audit on mutation events.

---

## 5. Global scope to simplify operations

---

## 6. Security is important
Security is an architectural concern.

Required:
- RBAC
- token management
Future:
- audit logging
- OIDC/SAML support
- signed artifacts
- provenance metadata
- SBOM support
- secret handling hygiene

---

## 7. Modular repository engines
Repository formats should be implemented as isolated modules.

Priorities:
- Maven -> 0
- PyPi -> 0
- npm -> 1
- OCI/Docker -> 1
- yum -> 1
- Generic blob -> 0
- debian -> 1
- go -> 2
- rust -> 2
- NuGet -> 3
- helm -> 3
- Others -> 4

Important to attempt to implement some kind of metadata sync whenever it is ideally feasible (example: rpm/apt).

---

# High-Level Architecture

## Components

### API
Responsibilities:
- authentication
- authorization
- routing
- request validation
- metrics

---

### Repository Service
Handles:
- artifact upload/download
- metadata
- repository rules
- retention policies
- storage abstraction

---

### Index/Search Service
Handles:
- metadata indexing
- full-text search
- dependency graph search
- tag/version lookup

Potential backends:
- PostgreSQL FTS
- OpenSearch
- Bleve

Essential to use FOSS projects to accelerate product development (Primary package format: rpm, primary platform to implement this on: RHEL/Rocky/Almalinux/OracleLinux)
---

### Auth Service
Responsibilities:
- OIDC
- LDAP
- SAML
- local users
- token issuance
- service accounts

---

### Background Workers
Handles:
- cleanup
- garbage collection
- replication
- indexing
- vulnerability scanning
- retention enforcement
- scheduled syncs on applicable repos

---

### Event Bus (This was generated by AI and I don't like how it sounds, smells alot, smells like operational trouble..)
Internal asynchronous event system.

Candidates:
- NATS
- Redis Streams
- Kafka

Used for:
- indexing
- notifications
- audit fanout
- webhook delivery
- replication

---

# Storage Model

## Metadata
Primary database:
- PostgreSQL

Use relational data for:
- repositories
- permissions
- users
- metadata
- artifact manifests
- audit records (NO! again, this was generated by AI, i don't like it in principle)

Do NOT store blobs in PostgreSQL.

---

## Blob Storage
Artifacts stored by content hash.

Example:
```text
sha256/ab/cd/<digest>
````

Features:

* deduplication (???? Are you sure this is feasible when this thing tries to scale? in an enterprise there's SAN-level dedupe so I feel this is irrelevant somehwat)
* integrity verification (To be configurable, i.e. can be turned off)
* immutable references (I don't even fucking know what this means)
* garbage collection (Yes, good)

---

# Core Features

## Repository Types

### Hosted

Internal artifact hosting.

### Proxy

Cache upstream repositories.

### Group

Aggregate multiple repositories into a virtual endpoint. (I'm not sure this feature is as important as it sounds)

---

## OCI Registry Support

First-class OCI support is mandatory.

Support:

* Docker images (Is the difference between those two more than just a philosophical one??)
* OCI artifacts (Is the difference between those two more than just a philosophical one??)
* Helm OCI (??? I don't fucking know what they are, priority is OCI)
* WASM OCI (??? I don't fucking know what they are, priority is OCI)
* SBOM OCI (??? I don't fucking know what they are, priority is OCI)
* signatures/attestations (??? I don't fucking know what they are, priority is OCI)

---

## Replication (Absolutely not a priority at MVP)

Repository replication between instances.

Modes:

* push
* pull
* scheduled
* real-time event driven

---

## Search (Not a priority at MVP)

Search should include:

* artifact name
* version
* tags
* checksums
* metadata
* labels
* provenance
* dependency relationships

---

## Audit Logging

Audit ALL:

* logins
* token operations
* artifact mutations
* permission changes
* admin actions

Audit logs must be tamper-resistant.

---

# Suggested Tech Stack

## Language

Go

---

## HTTP

Preferred: (No preferred, This is a TBD, can include NGINX as a dependency and play on this part) -> (Actually on this part it could work in a way similar to a controller -> worker, and nginx can route to speicifc repos under one virtual server, or a virutal server for each, tbd)

* chi
* echo
* fiber

Avoid overly magical frameworks.

---

## Database

PostgreSQL

Driver:

* pgx (?? tbd)

Preferred ORM/query layer:

* sqlc (???? based on what was this determined????)
* squirrel (???? based on what was this determined????)
* ent (???? based on what was this determined????)

Avoid heavy ORM abstraction leakage. (this is an overly generic statement and somewhat meaningless)

---

## Object Storage

Primary abstraction:

* S3-compatible APIs

Libraries:

* minio-go (Fuck minio, generic S3)
* aws-sdk-go-v2 (Cloud is not a first priority, on-prem first)

---

## Auth

Preferred:

* Ory (Uhm... No, I don't even fucking know what they are, I'm not trying to onboard technical debt @ ground zero)
* Zitadel integration (Uhm... No, I don't even fucking know what they are, I'm not trying to onboard technical debt @ ground zero)
* Dex (Uhm... No, I don't even fucking know what they are, I'm not trying to onboard technical debt @ ground zero)
* Keycloak compatibility (Actually no, keycloak is too complex to include in this project and I want to simplify)

JWT validation should be centralized.

---

## Messaging

Preferred:

* NATS (??? huh?)

Alternative:

* Redis Streams

Avoid premature Kafka complexity.

---

## Observability

Mandatory: (Actually not mandatory, not a priority in MVP)

* Prometheus metrics
* OpenTelemetry tracing
* structured logging

Preferred logging: (From here on out, discard any "preferred <insert component>" based on fucking what was "preferred" declared??????????)

* slog
* zap

---

## Frontend

Possible:

* React
* SvelteKit
* Vue

Requirements:

* API-driven
* RBAC-aware
* audit visibility
* repository browsing
* upload UX
* search-first design

---

# Repository Engine Guidelines

Each repository type should expose:

* upload handlers
* download handlers
* metadata extraction
* indexing logic
* validation logic
* retention hooks
* replication hooks

Repository engines should be isolated behind interfaces.

Avoid:

* giant switch statements
* repository-specific conditionals spread globally

---

# Performance Requirements

Target:

* High concurrency
* Large artifact support
* Streaming uploads/downloads
* Minimal memory amplification

Avoid:

* loading entire blobs into memory
* temporary disk copies where possible

Prefer:

* streaming pipelines
* zero-copy patterns where feasible

---

# Database Guidelines

## Migrations

Use:

* goose
* atlas
* migrate

Migrations must be:

* deterministic
* reversible where practical

---

## Query Design

Avoid:

* N+1 queries
* hidden ORM behavior
* implicit eager loading

Prefer:

* explicit joins
* pagination
* cursor-based APIs

---

# API Design

## Versioning

Use:

```text
/api/v1/
```

Version APIs explicitly.

---

## Pagination

Prefer cursor pagination over offsets.

---

## Errors

Consistent JSON error schema.

Example:

```json
{
  "error": {
    "code": "artifact_not_found",
    "message": "artifact does not exist"
  }
}
```

---

# Operational Requirements

## Deployment Targets

Support:

* Kubernetes
* Docker Compose
* bare metal
* VM deployments

---

## HA Expectations

Support:

* multiple API nodes
* rolling upgrades
* shared object storage
* shared database

Avoid:

* node-local critical state

---

## Backup Strategy

Critical backups:

* PostgreSQL
* object storage
* config secrets

Document restore procedures early.

---

# Security Requirements

## Secrets

Never store plaintext secrets.

Use:

* envelope encryption
* KMS integration where possible

---

## Supply Chain

Future support:

* Cosign
* Sigstore
* in-toto attestations
* SBOM ingestion

---

## Input Validation

All uploads must validate:

* size
* checksum
* content type
* path normalization

---

# Development Guidelines

## Code Organization

Example:

```text
/internal
  /api
  /auth
  /storage
  /repo
  /search
  /workers
  /events

/pkg
/cmd
/web
```

---

## Interfaces

Prefer small focused interfaces.

Avoid:

* god interfaces
* excessive abstraction

---

## Testing

Required:

* unit tests
* integration tests
* API tests

Strongly preferred:

* containerized integration testing

---

## Configuration

Preferred:

* YAML
* env overrides

Support:

* hot reload where reasonable

---

# Non-Goals (Initial Phase)

Do NOT initially implement:

* distributed SQL
* multi-region active-active
* blockchain anything
* custom storage engine
* excessive microservice fragmentation

Prioritize:

* correctness
* maintainability
* operational simplicity

---

# Initial MVP Scope

Phase 1 goals:

* Hosted repositories
* OCI registry support
* Generic blob repositories
* RBAC
* S3 backend
* PostgreSQL metadata
* Web UI
* Search
* Audit logging
* Token auth
* Repository proxying

---

# Long-Term Vision

The platform should evolve into:

* artifact repository
* OCI registry
* software supply chain platform
* provenance/security platform
* enterprise package distribution system

while remaining operationally lightweight compared to legacy enterprise artifact managers.

```
```
